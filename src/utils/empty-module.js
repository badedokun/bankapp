/**
 * Empty Module
 * Used by Metro bundler to replace browser-only packages on mobile platforms
 * This prevents crashes when browser-only packages are imported
 */

module.exports = {};
module.exports.default = {};
